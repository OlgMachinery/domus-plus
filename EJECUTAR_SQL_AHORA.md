# 🚀 Ejecutar SQL en Supabase - Instrucciones Rápidas

## ✅ Pasos (2 minutos)

1. **Abre el dashboard de Supabase:**
   - Ve a: https://supabase.com/dashboard/project/lpmslitbvlihzucorenj
   - O simplemente ve a tu dashboard y selecciona el proyecto

2. **Abre SQL Editor:**
   - En el menú lateral, clic en **"SQL Editor"** (icono de base de datos)

3. **Crea una nueva query:**
   - Clic en el botón **"New Query"** (arriba a la derecha)

4. **Copia el SQL:**
   - Abre el archivo `supabase/schema.sql` de este proyecto
   - Selecciona TODO el contenido (Cmd+A o Ctrl+A)
   - Copia (Cmd+C o Ctrl+C)

5. **Pega en el editor:**
   - Pega el SQL en el editor de Supabase (Cmd+V o Ctrl+V)

6. **Ejecuta:**
   - Clic en el botón **"Run"** (verde, arriba a la derecha)
   - O presiona **Cmd+Enter** (Mac) o **Ctrl+Enter** (Windows/Linux)

7. **Verifica:**
   - Deberías ver: "Success. No rows returned"
   - En el menú lateral, ve a **"Table Editor"**
   - Deberías ver todas las tablas creadas (users, families, transactions, etc.)

## 📋 Checklist

- [ ] Dashboard de Supabase abierto
- [ ] SQL Editor abierto
- [ ] Nueva query creada
- [ ] SQL copiado de `supabase/schema.sql`
- [ ] SQL pegado en el editor
- [ ] Query ejecutada exitosamente
- [ ] Tablas verificadas en Table Editor

## ⚠️ Si hay errores

- Verifica que copiaste **TODO** el contenido del archivo SQL
- Asegúrate de que no haya texto adicional
- Revisa los mensajes de error en Supabase

## ✅ Una vez completado

Después de ejecutar el SQL, puedes iniciar la aplicación:

```bash
cd frontend
npm run dev
```

¡Y listo! 🎉
